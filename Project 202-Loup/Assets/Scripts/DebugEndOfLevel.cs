using UnityEngine;
using System.Collections;

public class DebugEndOfLevel : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //this.transform.position = new Vector3(GameObject.Find("_Manager").GetComponent<SpeedManager>().LevelSize, 0, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
